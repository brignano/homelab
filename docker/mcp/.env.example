# Grafana service-account token for mcp-grafana.
# Create in Grafana: Administration -> Users and access -> Service accounts ->
# Add service account -> role MUST be "Viewer" (read-only is the security
# guarantee) -> Add service account token -> copy the value here.
GRAFANA_SERVICE_ACCOUNT_TOKEN=changeme-grafana-viewer-service-account-token
