Write a new dated entry in docs/setup-log.md.

Ask me for:
1. A short title for what was done (e.g. "Added Jellyfin stack")
2. The goal
3. The steps taken (bullet points are fine)
4. Any issues encountered and how they were resolved
5. Next steps

Then prepend a filled-in entry to docs/setup-log.md using today's date and the template defined at the top of that file. Do not remove or modify existing entries.
